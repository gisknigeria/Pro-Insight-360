{
  "name": "api",
  "version": "0.0.1",
  "description": "Pro-Insight 360 Express backend",
  "private": true,
  "license": "UNLICENSED",
 "scripts": {
  "start": "node server.js",
  "start:prod": "node server.js",
  "build": "prisma migrate deploy --schema=prisma/schema.prisma",
"postinstall": "prisma generate --schema=prisma/schema.prisma",
  "migrate:deploy": "prisma migrate deploy --schema=prisma/schema.prisma"
},
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "prisma": "^5.15.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
